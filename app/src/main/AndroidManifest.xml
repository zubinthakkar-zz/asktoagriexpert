<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zubinthakkar.asktoagriexpert">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name" />
        <activity
            android:name=".FarmerLogin"
            android:label="@string/title_activity_farmer_login" />
        <activity
            android:name=".ExpertLogin"
            android:label="@string/title_activity_expert_login" />
        <activity
            android:name=".FarmerDashboard"
            android:label="@string/title_activity_farmer_dashboard" />
        <activity
            android:name=".ExpertDashboard"
            android:label="@string/title_activity_expert_dashboard" />
        <activity
            android:name=".FarmerRegistration"
            android:label="@string/farmerregistration" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExpertRegistration" />
        <activity android:name=".query_list" />
        <activity android:name=".ExpertAnswered"></activity>
    </application>

</manifest>